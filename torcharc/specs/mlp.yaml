modules:
  mlp:
    Sequential:
      - Linear:
          in_features: 128
          out_features: 64
      - ReLU:
      - Linear:
          in_features: 64
          out_features: 10

graph:
  # TODO this can be string or list
  input: x
  modules:
    # TODO shortcut: list -> args, dict -> kwargs
    mlp: [x]
  # TODO this can be string, list or dict to reflect return formats
  output: mlp
