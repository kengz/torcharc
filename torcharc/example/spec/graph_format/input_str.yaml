modules:
  mlp:
    Sequential:
      - Linear:
          in_features: 128
          out_features: 64
      - ReLU:
      - Linear:
          in_features: 64
          out_features: 10

graph:
  # use str to name input placeholder
  input: x
  modules:
    mlp: [x]
  output: mlp
